apiVersion: v1
kind: Service
metadata:
    name: "wubot2-dddc"
    labels:
        app: "wubot2-dddc"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "wubot2-dddc"